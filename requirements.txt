# Verified against torch 2.5.1 / torchvision 0.20.1 on CPU (Python 3.11).
# The tiny-CPU smoke and the unit tests need only torch, torchvision, pillow,
# fastapi, pydantic and pytest. The rest are for full training / serving.
torch>=2.0
torchvision>=0.15
numpy>=1.24
pillow>=9.0
matplotlib>=3.6
PyYAML>=6.0
tqdm>=4.62
albumentations>=1.3
pycocotools>=2.0.6
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0
python-multipart>=0.0.6
mlflow>=2.0
pytest>=7.0
